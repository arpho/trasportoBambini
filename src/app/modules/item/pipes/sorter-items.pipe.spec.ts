import { SorterItemsPipe } from './sorter-items.pipe';

describe('SorterItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SorterItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
